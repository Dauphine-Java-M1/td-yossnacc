Yossef Naccache 

TD Itérable
Exo1:

Q3) Il faut implémenter l'interface iterable qui a une méthode iterator qui renvoie un Iterable<Integer>

Q4) Cette classe  est utilisée pour implémenter une collection non modifiable.
	On implémente les méthodes get () et size(). 
	Ce sont les méthodes qui permettent d'avoir une liste non mutable.
			
			